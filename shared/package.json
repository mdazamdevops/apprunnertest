{
  "name": "@crazytrainai/shared",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/schema.ts --format esm,cjs --dts --out-dir dist --clean"
  },
  "dependencies": {
    "drizzle-orm": "^0.31.2",
    "zod": "^3.22.4",
    "drizzle-zod": "^0.6.0"
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.3.3"
  }
}
